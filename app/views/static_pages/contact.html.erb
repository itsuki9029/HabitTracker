<div class="container my-5">
  <h1 class="text-center mb-4">お問い合わせ</h1>
  
  <p class="text-center">ご質問やお問い合わせがありましたら、以下のフォームをご利用ください。</p>
  
  <div class="row justify-content-center">
    <div class="col-md-8">
      <%= form_with url: contact_path, method: :post, local: true, class: "needs-validation", novalidate: true do |f| %>
        <div class="mb-3">
          <%= f.label :name, "お名前", class: "form-label text-white" %>
          <%= f.text_field :name, class: "form-control", required: true, placeholder: "例: 山田 太郎" %>
        </div>

        <div class="mb-3">
          <%= f.label :email, "メールアドレス", class: "form-label text-white" %>
          <%= f.email_field :email, class: "form-control", required: true, placeholder: "例: example@example.com" %>
        </div>

        <div class="mb-3">
          <%= f.label :message, "お問い合わせ内容", class: "form-label text-white" %>
          <%= f.text_area :message, class: "form-control", rows: 5, required: true, placeholder: "お問い合わせ内容をご記入ください。" %>
        </div>

        <div class="text-center">
          <%= f.submit "送信", class: "btn submit-button" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
