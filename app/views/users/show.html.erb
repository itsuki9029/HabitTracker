<h1>プロフィール</h1>
<div class="profile">
  <% if @user.profile_image.present? %>
    <%= image_tag @user.profile_image.url, alt: "プロフィール画像", size: "150x150" %>
  <% else %>
    <%= image_tag "default_avatar.png", alt: "デフォルト画像", size: "150x150" %>
  <% end %>

  <p>ユーザー名: <%= @user.username %></p>
  <p>メールアドレス: <%= @user.email %></p>
  <p>フォロー数: <%= @user.following.count %></p>
  <p>フォロワー数: <%= @user.followers.count %></p>

  <!-- ログインユーザーが自分のプロフィールを表示している場合のみボタンを表示 -->
  <% if current_user == @user %>
    <%= link_to 'フォロー 一覧', following_user_path(@user), class: 'btn btn-primary mb-2' %>
    <%= link_to 'フォロワー 一覧', followers_user_path(@user), class: 'btn btn-primary mb-2' %>
    <%= link_to 'プロフィール編集', edit_user_path(@user), class: 'btn btn-primary mb-2' %>
  <% end %>
</div>
